-- ============================================
-- 📌 SQL PRACTICE QUESTIONS (UNION)
-- ============================================

-- 🗂️ TABLE: Students_A

CREATE TABLE Students_A (
student_id INT,
name VARCHAR(50)
);

-- 🗂️ TABLE: Students_B

CREATE TABLE Students_B (
student_id INT,
name VARCHAR(50)
);

-- ============================================
-- 📊 SUMMARY
-- ============================================

-- Total Questions Practiced: 5

-- Topics Covered:
-- UNION basics
-- Removing duplicates
-- ORDER BY with UNION
-- Filtering with UNION
-- Exclusion using conditions

-- Key Learnings:
-- 1. UNION combines results from multiple queries
-- 2. UNION removes duplicate rows automatically
-- 3. Both queries must have same number of columns
-- 4. Column data types must be compatible
-- 5. ORDER BY is applied at the end of UNION

-- ============================================
-- END OF NOTES
-- ============================================
