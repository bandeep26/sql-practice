-- ============================================
-- 📌 SQL PRACTICE QUESTIONS (DML)
-- ============================================

-- 🗂️ TABLE: Employees

CREATE TABLE Employees (
emp_id INT PRIMARY KEY,
name VARCHAR(50),
department VARCHAR(50),
salary DECIMAL(10,2),
city VARCHAR(50)
);

-- ============================================
-- 📊 SUMMARY
-- ============================================

-- Total Questions Practiced: 13

-- Topics Covered:
-- INSERT
-- SELECT (WHERE, AND, OR, LIKE, BETWEEN)
-- UPDATE
-- DELETE

-- Key Learnings:
-- 1. DML is used to manipulate data inside tables
-- 2. WHERE clause is critical for filtering
-- 3. UPDATE and DELETE without WHERE affect all rows
-- 4. SELECT helps in data analysis and retrieval

-- ============================================
-- END OF NOTES
-- ============================================
